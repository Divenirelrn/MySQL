
方式一：
插入语句：
    INSERT INTO 表名（列名，...）
    VALUES(值1，...)

插入值的类型与列的类型要兼容
列名与值可以成对省略
列数与值的个数必须一致
可省略列名，默认为所有列

INSERT INTO beauty(id, name, gender, borndate)
VALUES(13,'唐艺昕','女','1990-4-23')


方式二：
插入语句：
    INSERT INTO 表名
    SET 列名=值, 列名=值, ...


两种方式比较：
    方式一支持多行，方式二不支持
    方式一支持子查询，方式二不支持


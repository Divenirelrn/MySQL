
外部的查询语句称为主查询（外查询）
内部的查询语句称为子查询（内查询，嵌套查询）

分类：
    按子查询出现的位置：
        select后面
        from后面
        where或having后面
        exists后面

    按功能（结果集的行列数）：
        标量子查询（一行一列）
        列子查询（一列）
        行子查询（一行）
        表子查询（多行多列）

select只支持标量子查询
from支持表子查询
where或having支持标量、列、行子查询
exits支持表子查询
